import { TodoCreatedPipePipe } from './todo-created-pipe-pipe';

describe('TodoCreatedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TodoCreatedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
