import { PercentColor } from './percent-color';

describe('PercentColor', () => {
  it('should create an instance', () => {
    const directive = new PercentColor();
    expect(directive).toBeTruthy();
  });
});
